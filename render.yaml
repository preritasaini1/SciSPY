services:
  - type: web
    name: scispy-agent
    runtime: python
    buildCommand: |
      python3.10 -m pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: streamlit run app_frontend.py --server.port $PORT --server.address 0.0.0.0
